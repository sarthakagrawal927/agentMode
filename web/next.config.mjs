/** @type {import('next').NextConfig} */
const nextConfig = {
  // Note: Disabling strict mode is not recommended for development
  // as it helps catch potential issues
  reactStrictMode: false,
  swcMinify: true,
};

export default nextConfig;
